# Backend env template — copy to .env and fill values
# Atlas project: ecommerce-backend (Cluster0) → DB "fluidApp"
# Existing collections: categories, departments, machines, machineapprovals,
# qamachineentries, permissionconfigs, roles, users
# (project name is misleading; this cluster actually hosts fluidApp data.)

PORT=
MONGO_URI=

# JWT
ACCESS_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=
REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_EXPIRY=
